{
	"name": "limbus-company-helper",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run src/index.ts dev",
		"debug": "bun build src/team-code-debugger.ts --outdir ./public --watch",
		"test": "bun test --watch",
		"start": "bun run src/index.ts start",
		"sync": "bun run src/index.ts sync"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}

{
	"name": "limbus-company-helper",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run src/app.ts dev",
		"debug": "bun build src/team-code-debugger.ts --outdir ./docs/team-code --watch",
		"test": "bun test --watch",
		"start": "bun run src/app.ts start",
		"sync": "bun run src/app.ts sync"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
